package IL;

import java.util.HashMap;
import java.util.List;

public class Interface {

/*
    public static String viewDeliveries (){

     return DeliveryController.getInstance().toString();
    }
    public static void addDelivery(String date, String hour, String tid, String driverName, String source, int weightBeforeGo, List<String> numberedFiles, HashMap<String,String> adresses, HashMap<String,HashMap<String,Integer>> products ){
        Callback c=DeliveryController.getInstance().addDelivery(date,hour,tid,driverName,source,weightBeforeGo,numberedFiles,adresses,products);
        c.run(date,hour,tid,driverName,source,weightBeforeGo,numberedFiles,adresses,products);
    }

    public static void addDriver(String name,License license){
        DriverController.getInstance().addDriver(license,name);
    }
    public static void addTruck(String id, int weight, int maxWeight, String model){
        TruckController.getInstance().addTruck(id,weight,maxWeight,model);
    }
    public static void addDestination(String address, String contact, String phone, String area){
        DestController.getInstance().addDestination(address,contact,phone,area);
    }

*/
}

//Final version