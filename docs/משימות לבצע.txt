1) תמיכה של ה DATABASE בשמירת תאריכים באופן חדש בפורמט 2020-06-23
2) תמיכה של יצירת ימים מחדש מהDATABASE באותו פורמט (כיוון שני)
3)יצירת טבלה ותחזוקה שמתארת את התאריך בו נמצאת המערכת
4) שינוי כל התלויות של פונקציות בכך שהDAY במיקום ה - 0 הוא ראשון (לא בהכרח יהיה) - 
שימוש ב DAY_IN_WEEK שנמצא ב STORE CONTROLLER


Stock+Providers
1) change product, report, order to include storeNum in signatures of functions 
and change the functions' behaviour appropriately (to depend on a store סניף)
